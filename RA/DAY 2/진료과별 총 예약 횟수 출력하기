SELECT MCDP_CD AS 진료과코드, COUNT(PT_NO) AS 5월예약건수
FROM (
    SELECT * FROM APPOINTMENT WHERE DATE_FORMAT(APNT_YMD,"%Y")=2022 AND DATE_FORMAT(APNT_YMD,"%m")=5
) A
GROUP BY MCDP_CD
ORDER BY 5월예약건수 ASC, 진료과코드 ASC
;
